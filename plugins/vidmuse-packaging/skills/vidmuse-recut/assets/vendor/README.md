Legacy-only local runtime staging. New Registry-first projects keep approved,
exact-version CDN URLs and record them in `effect-sources.json`; do not copy a
second GSAP runtime into the project by default.
