interface:
  display_name: "VidMuse IP"
  short_description: "Create flexible audio-driven IP films"
  default_prompt: "Use $vidmuse-ip to compile flexible prompts from narration beats, generate the IP film, and place candidate clips in Timeline immediately for my review."
