# Requirements

- `vidmuse` on PATH
- authenticated VidMuse account
- active plan/credits for model calls
- network access
- `ffmpeg` and `ffprobe`
- Node.js 18+

Check with:

```bash
node <SKILL_DIR>/scripts/resolve.mjs --doctor
```

No provider-specific keys or local AI installations are part of this skill.
